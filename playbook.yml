---
- name: install tomcat and deploy the application
  hosts: Deploy
  copy:
    src: /var/lib/jenkins/workspace/Pipeline/com/hello/HelloWorld/0.0.1-SNAPSHOT/HelloWorld-0.0.1-{version}.jar
    dest: /opt/tomcat/latest/webapps
